/*
 * This is a usage example of while statement in the comp25 language 
 */
program main
begin 
    procedure main()
    begin
        var x: int
    in
        x := 0;
        while expi(x, 2) < 20 do 
            print_string("x: ");
            print_int(expi(x, 2));
            print_string("\n");
            x := x + 1
        od
    end
end

