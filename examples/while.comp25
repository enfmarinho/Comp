/*
 * This is a usage example of while statement in the comp25 language 
 */
program main
begin 
    procedure main()
    begin
        var x: int
    in
        x := 0;
        while x < 10 do 
            x := x + 1
        od
    end
end

